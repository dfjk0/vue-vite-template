{
  "name": "vite-vml",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "clean": "rimraf dist",
    "reset": "rimraf dist && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "vite": "^2.8.4",
    "vite-plugin-html": "^3.1.0"
  }
}
